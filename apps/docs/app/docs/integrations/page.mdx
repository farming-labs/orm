---
title: "Integrations"
description: "How Farming Labs ORM fits into Prisma, Drizzle, Kysely, TypeORM, Sequelize, SQL-first, Supabase, Firestore, and MongoDB-oriented workflows."
order: 6
icon: package
---

# Integrations

Farming Labs ORM has two layers:

- `@farming-labs/orm` defines the schema and query API
- runtime packages execute that API against the app’s actual stack

Hosted PostgreSQL platforms such as Supabase fit through that same PostgreSQL
layer rather than needing a separate database model.

If you want the quickest “what works where?” answer, start with the
[Support Matrix](/docs/integrations/support-matrix).

## Support matrix

| Layer               | PostgreSQL | MySQL | SQLite | MongoDB | Notes                                     |
| ------------------- | ---------- | ----- | ------ | ------- | ----------------------------------------- |
| Prisma generation   | Yes        | Yes   | Yes    | No      | `postgresql`, `mysql`, `sqlite` providers |
| Drizzle generation  | Yes        | Yes   | Yes    | No      | `pg`, `mysql`, `sqlite` dialects          |
| Safe SQL generation | Yes        | Yes   | Yes    | No      | `postgres`, `mysql`, `sqlite` dialects    |
| Memory runtime      | N/A        | N/A   | N/A    | N/A     | In-memory driver for tests and demos      |
| Prisma runtime      | Yes        | Yes   | Yes    | No      | `@farming-labs/orm-prisma`                |
| Drizzle runtime     | Yes        | Yes   | Yes    | No      | `@farming-labs/orm-drizzle`               |
| Direct SQL runtime  | Yes        | Yes   | Yes    | No      | `@farming-labs/orm-sql`                   |
| TypeORM runtime     | Yes        | Yes   | Yes    | No      | `@farming-labs/orm-typeorm`               |
| Sequelize runtime   | Yes        | Yes   | No     | No      | `@farming-labs/orm-sequelize`             |
| MongoDB runtime     | No         | No    | No     | Yes     | `@farming-labs/orm-mongo`                 |
| Mongoose runtime    | No         | No    | No     | Yes     | `@farming-labs/orm-mongoose`              |
| Kysely runtime      | Yes        | Yes   | Yes    | No      | `@farming-labs/orm-kysely`                |
| Firestore runtime   | No         | No    | No     | Yes     | `@farming-labs/orm-firestore`             |

Supabase is already covered through the PostgreSQL layer:

- generated Prisma / Drizzle / SQL output still works
- the live ORM runtime works through a PostgreSQL client connected to Supabase
- setup helpers such as `pushSchema(...)` and `bootstrapDatabase(...)` still work through that PostgreSQL path

Firestore is now a first-class document runtime in this repo through
`@farming-labs/orm-firestore`.

- it works with server-side Firestore clients such as `@google-cloud/firestore`
  and `firebase-admin`
- it keeps the same schema and query API surface as the SQL and Mongo runtimes
- relation selections resolve through follow-up ORM queries rather than native joins
- `pushSchema(...)` and `applySchema(...)` are intentional no-ops because
  Firestore does not have SQL-style DDL to apply

## Scalar field coverage

The current repo verifies the scalar builders across the full live runtime
matrix:

| Runtime / stack    | PostgreSQL | MySQL | SQLite | MongoDB | `integer()` | `json()` | `enumeration()` | `bigint()` | `decimal()` |
| ------------------ | ---------- | ----- | ------ | ------- | ----------- | -------- | --------------- | ---------- | ----------- |
| Memory runtime     | N/A        | N/A   | N/A    | N/A     | Yes         | Yes      | Yes             | Yes        | Yes         |
| Prisma runtime     | Yes        | Yes   | Yes    | No      | Yes         | Yes      | Yes             | Yes        | Yes         |
| Drizzle runtime    | Yes        | Yes   | Yes    | No      | Yes         | Yes      | Yes             | Yes        | Yes         |
| Kysely runtime     | Yes        | Yes   | Yes    | No      | Yes         | Yes      | Yes             | Yes        | Yes         |
| Direct SQL runtime | Yes        | Yes   | Yes    | No      | Yes         | Yes      | Yes             | Yes        | Yes         |
| TypeORM runtime    | Yes        | Yes   | Yes    | No      | Yes         | Yes      | Yes             | Yes\*      | Yes         |
| Sequelize runtime  | Yes        | Yes   | No     | No      | Yes         | Yes      | Yes             | Yes        | Yes         |
| MongoDB runtime    | No         | No    | No     | Yes     | Yes         | Yes      | Yes             | Yes        | Yes         |
| Mongoose runtime   | No         | No    | No     | Yes     | Yes         | Yes      | Yes             | Yes        | Yes         |
| Firestore runtime  | No         | No    | No     | Yes     | Yes         | Yes      | Yes             | Yes        | Yes         |

Those checks are not generator-only. They run the live runtime query layer
against real local database instances and real client stacks for each supported
dialect combination.

That same real local matrix now also verifies `orm.$driver.capabilities`,
including transaction-scoped access in the live SQL and Prisma runtimes.

For SQLite bigint coverage, the local `node:sqlite` clients enable big-int read
mode so the values stay as real `bigint` outputs instead of truncating into
JavaScript `number`.

Use this command to run the whole local matrix:

```bash title="terminal"
pnpm test:local
```

## Native relation loading

The current runtime uses 2 strategies:

- native relation loading where the backend can translate the query shape directly
- fallback relation resolution for shapes that still need the shared runtime layer

Today that means:

| Runtime / stack    | Native path today                                                                                                               | Fallback today                                                           |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| Prisma runtime     | direct nested Prisma `select` for supported relation branches, including simple explicit join-table `manyToMany(...)` traversal | relation branches with extra relation-level filters, ordering, or paging |
| Drizzle runtime    | inherited SQL single-query loading for singular chains and simple collection branches without relation-level modifiers          | relation branches with extra relation-level filters, ordering, or paging |
| Kysely runtime     | inherited SQL single-query loading for singular chains and simple collection branches without relation-level modifiers          | relation branches with extra relation-level filters, ordering, or paging |
| Direct SQL runtime | single SQL statement for singular chains and simple collection branches without relation-level modifiers                        | relation branches with extra relation-level filters, ordering, or paging |
| TypeORM runtime    | inherited SQL single-query loading for singular chains and simple collection branches without relation-level modifiers          | relation branches with extra relation-level filters, ordering, or paging |
| Sequelize runtime  | inherited SQL single-query loading for singular chains and simple collection branches without relation-level modifiers          | relation branches with extra relation-level filters, ordering, or paging |
| Mongo / Mongoose   | not yet                                                                                                                         | shared relation resolver                                                 |
| Firestore          | not yet                                                                                                                         | shared relation resolver                                                 |

The important nuance is that the fallback column does **not** mean the backend
is fundamentally missing relation support.

- SQL-backed runtimes can support much richer native joins than the first
  singular path implemented here
- Prisma can support more native nested relation shapes than the current direct
  branch translation
- MongoDB and Mongoose can grow a native aggregation / `$lookup` path later

So the current matrix is best read as: **what this repo natively translates
today**, not **the theoretical limit of the backend**.

\* The TypeORM suite fully verifies bigint round-trips on PostgreSQL and MySQL.
The SQLite-family TypeORM path currently uses SQL.js smoke coverage in this
repo, so bigint precision there still depends on the concrete TypeORM sqlite
driver you choose.

## How to choose

- Use **Prisma generation** if your app already owns `schema.prisma`
- Use **Drizzle generation** if your app wants generated Drizzle tables
- Use **safe SQL generation** if you want SQL files directly
- Use **Prisma runtime** if your app already uses `PrismaClient`
- Use **Drizzle runtime** if your app already uses Drizzle
- Use **Kysely runtime** if your app already uses Kysely
- Use **TypeORM runtime** if your app already uses a TypeORM `DataSource`
- Use **Sequelize runtime** if your app already uses Sequelize
- Use **direct SQL runtime** if you want to talk to SQLite, MySQL, or PostgreSQL directly
- Use **Firestore runtime** if your app uses a server-side Firestore client
- Use **MongoDB runtime** if your app uses the native `mongodb` client
- Use **Mongoose runtime** if your app already uses Mongoose models

## What “supported” means

When a runtime is marked live, the unified ORM methods already translate into
real calls for that stack.

Examples:

- `@farming-labs/orm-prisma` translates into Prisma delegate calls
- `@farming-labs/orm-drizzle` translates into Drizzle-backed queries
- `@farming-labs/orm-kysely` translates into Kysely-backed queries
- `@farming-labs/orm-sql` translates into direct SQL operations
- `@farming-labs/orm-typeorm` translates into TypeORM-backed relational operations
- `@farming-labs/orm-sequelize` translates into Sequelize-backed relational operations
- `@farming-labs/orm-firestore` translates into Firestore document operations
- `@farming-labs/orm-mongo` translates into native `mongodb` collection operations
- `@farming-labs/orm-mongoose` translates into Mongoose model operations

## Guides

<HoverLink
  href="/docs/integrations/prisma"
  title="Prisma"
  description="See how Farming Labs ORM generates Prisma schema output and translates the unified runtime into a real PrismaClient."
  linkLabel="Open Prisma guide"
>
  Prisma integration
</HoverLink>

<HoverLink
  href="/docs/integrations/drizzle"
  title="Drizzle"
  description="Learn how generated Drizzle schema files and the live Drizzle runtime fit into PostgreSQL, MySQL, and SQLite apps."
  linkLabel="Open Drizzle guide"
>
  Drizzle integration
</HoverLink>

<HoverLink
  href="/docs/integrations/kysely"
  title="Kysely"
  description="See how a real Kysely instance can power the unified Farming ORM runtime without changing library-level storage code."
  linkLabel="Open Kysely guide"
>
  Kysely integration
</HoverLink>

<HoverLink
  href="/docs/integrations/typeorm"
  title="TypeORM"
  description="See how a real TypeORM DataSource can power the unified Farming ORM runtime without rewriting package-level storage code."
  linkLabel="Open TypeORM guide"
>
  TypeORM integration
</HoverLink>

<HoverLink
  href="/docs/integrations/sequelize"
  title="Sequelize"
  description="See how a real Sequelize instance can power the unified Farming ORM runtime without rewriting package-level storage code."
  linkLabel="Open Sequelize guide"
>
  Sequelize integration
</HoverLink>

<HoverLink
  href="/docs/integrations/sql-databases"
  title="SQL Databases"
  description="Explore the direct SQL runtime, safe SQL generation, and the PostgreSQL, MySQL, and SQLite runtime matrix."
  linkLabel="Open SQL guide"
>
  SQL integration
</HoverLink>

<HoverLink
  href="/docs/integrations/supabase"
  title="Supabase"
  description="See how Farming Labs ORM uses the PostgreSQL layer to work with Supabase without creating a separate database model."
  linkLabel="Open Supabase guide"
>
  Supabase integration
</HoverLink>

<HoverLink
  href="/docs/integrations/firestore"
  title="Firestore"
  description="See how Farming Labs ORM translates one schema and one storage layer into Firebase / Firestore server-side workflows."
  linkLabel="Open Firestore guide"
>
  Firestore integration
</HoverLink>

<HoverLink
  href="/docs/integrations/mongodb"
  title="MongoDB"
  description="See how the native MongoDB and Mongoose runtimes translate the unified query layer into document-database operations."
  linkLabel="Open MongoDB guide"
>
  MongoDB integration
</HoverLink>
